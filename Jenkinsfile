pipeline {
    agent any

    stages {
        stage('NPM Build') {
            steps{
                echo 'seocnd test é'
                bat 'npm install'
                bat 'npm start'
            }
        }
    }
}