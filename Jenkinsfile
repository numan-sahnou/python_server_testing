pipeline {
    agent any

    stages {
        stage('NPM Build') {
            steps{
                echo 'seocnd test'
                bat 'npm install'
                bat 'npm start'
            }
        }
    }
}