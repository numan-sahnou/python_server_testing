pipeline {
    agent any

    stages {
        stage('NPM Build') {
            steps{
                echo 'test'
                bat 'npm install'
                bat 'npm start'
            }
        }
    }
}